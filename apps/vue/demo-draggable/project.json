{
  "name": "demo-draggable",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/vue/demo-draggable/src",
  "tags": ["draggable", "vue", "demo"],
  "targets": {
    "publish-demo": {
      "cache": false,
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "vite build --outDir=../../../deploy/demo-draggable/vue --emptyOutDir"
        ],
        "cwd": "{projectRoot}"
      }
    }
  }
}
