/**
 * Shell locales that ship with the root entry (English default).
 * Domain locales: `<domain>/locale/locale.<lang>.ts` (compat: `locale/index.ts` → `locale.en`).
 * Aggregate packs: `locale/locale.<lang>.ts` (`MAP_CORE_LOCALE_EN` / `MAP_CORE_LOCALE_VI`).
 */
export * from './shell.en';
