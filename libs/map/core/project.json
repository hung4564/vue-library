{
  "name": "map-core",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/map/core/src",
  "projectType": "library",
  "tags": ["map"],
  "targets": {
    "ts-check": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "commands": ["vue-tsc -p tsconfig.lib.json"],
        "forwardAllArgs": false,
        "description": "Type check with vue-tsc"
      }
    }
  }
}
