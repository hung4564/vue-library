# Draggable **1.2.x** (prep)

Draft SemVer / docs pack for the next **minor** line after published **`<!-- docs-ver:draggable.line -->1.1.x<!-- /docs-ver:draggable.line -->`** (`<!-- docs-ver:draggable.exact -->1.1.0<!-- /docs-ver:draggable.exact -->`).

| Status | Value |
|--------|--------|
| Target versions | `@hungpvq/draggable` / `vue-draggable` / `react-draggable` → **`1.2.0`** (fixed release group) |
| Bump type | **minor** (additive only — no breaking protocol) |
| Published today | synced from `package.json` via `scripts/sync-draggable-docs-version.js` |
| Adapter peers after release | `@hungpvq/draggable` **`~1.2.0`** |
| Map follow-up | Bump map-dataset peers on `vue-draggable` / `react-draggable` to **`~1.2.0`** in the same map release (or document temporary ranges if staggered) |

Related: [SemVer checklist](../../README.md#checklist-semver--breaking-change) · [Stable API](../stable-api.md) · [Testing](../testing.md) · [a11y](../a11y.md)

Release orchestrator (version → sync docs → site → tag `draggable@<version>` → push):

```bash
npm run draggable:release
# or
node scripts/release-group.js draggable minor
```

---

## Peer matrix (after 1.2.0 ships)

| Package | `exports` | Peer notes |
|---------|-----------|------------|
| `@hungpvq/draggable@1.2.x` | `.` + `./style.css` | `@hungpvq/shared-store` |
| `@hungpvq/vue-draggable@1.2.x` | `.` + `./style.css` | `draggable ~1.2.0`, `vue`, `vue-draggable-resizable` |
| `@hungpvq/react-draggable@1.2.x` | `.` + `./style.css` | `draggable ~1.2.0`, React 18, `react-rnd` |

---

## Migration **1.1 → 1.2**

Expected: **none** for Stable shell consumers once peers move to `~1.2.0`.

---

## Release-day checklist

Prefer `npm run draggable:release` (see `scripts/release-group.js`). Manual fallback:

1. [ ] `npm run draggable:test` / `draggable:build`
2. [ ] `node scripts/release-group.js draggable [specifier]`
3. [ ] Confirm GitHub Actions **Publish** on tag `draggable@*`
4. [ ] Confirm [demo-draggable](https://hung4564.github.io/demo-draggable/) updated
5. [ ] Update this page status → **shipped**; stub `v1.3.md` if needed
6. [ ] Align map peers if required
