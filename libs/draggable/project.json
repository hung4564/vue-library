{
  "name": "draggable",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/draggable/src",
  "projectType": "library",
  "tags": ["draggable"],
  "// targets": "to see all targets run: nx show project draggable--web",
  "targets": {
    "ts-check": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "commands": ["vue-tsc -p tsconfig.lib.json --noEmit"],
        "forwardAllArgs": false,
        "description": "Type check with tsc"
      }
    }
  }
}
