{
  "name": "map-basemap",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/map-basemap/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project map-basemap --web",
  "targets": {
    "ts-check": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "commands": ["tsc -p tsconfig.lib.json --noEmit"],
        "forwardAllArgs": false,
        "description": "Type check with tsc"
      }
    }
  }
}
