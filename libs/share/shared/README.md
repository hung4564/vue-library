# Share

Vue-oriented utilities (`@hungpvq/shared`). **0.x — public surface is not SemVer-locked** (barrel `export *`); treat as unstable until a future Stable allowlist.

## Installation

```bash
npm i @hungpvq/shared
# peer
npm i vue@^3.3.4
```

Requires **Vue 3** as a peer (`^3.3.4`).
